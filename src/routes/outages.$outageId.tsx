import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowLeft, BrainCircuit, Clock, RadioTower, ShieldCheck } from "lucide-react";
import { AppShell, PageHeader } from "@/components/gw/app-shell";
import { ActivityTimeline } from "@/components/gw/activity-timeline";
import { CopyHash } from "@/components/gw/copy-hash";
import { StatusBadge } from "@/components/gw/status-badge";
import { Button } from "@/components/ui/button";
import { Progress } from "@/components/ui/progress";
import { useGrid } from "@/lib/grid-store";

export const Route=createFileRoute("/outages/$outageId")({component:OutageDetailRoute}); function OutageDetailRoute(){return <AppShell><Detail/></AppShell>}
function Detail(){const {outageId}=Route.useParams(); const {outages}=useGrid(); const o=outages.find(x=>x.id===outageId)??outages[0]; const state=o.status==="restored"?"on":o.status==="pending"?"conflict":"outage"; const steps=[{id:"1",title:"First meter report",description:`A registered meter in ${o.area} reported Power OFF.`,time:o.startTime,state:"done" as const},{id:"2",title:"Neighbourhood reports collected",description:`${o.reports} signed readings were compared.`,time:"+2 min",state:"done" as const},{id:"3",title:"Consensus reached",description:`${o.consensus}% of trusted meters agreed.`,time:"+3 min",state:"done" as const},{id:"4",title:o.verified?"Recorded on HSK Chain":"Awaiting verification",description:o.verified?`Event confirmed in block #${o.block}.`:"More trusted readings are needed.",time:"+4 min",state:(o.verified?"done":"current") as "done"|"current"},{id:"5",title:"AI restoration estimate",description:o.prediction,time:"+5 min",state:(o.verified?"current":"upcoming") as "current"|"upcoming"}]; return <><Button variant="ghost" size="sm" asChild className="mb-4"><Link to="/outages"><ArrowLeft/>Back to outages</Link></Button><PageHeader eyebrow={o.id} title={`${o.area} outage event`} description="Detailed consensus evidence, AI prediction and simulated on-chain verification." actions={<StatusBadge state={state} label={o.status==="active"?"Verified outage":o.status==="restored"?"Restored":"Pending verification"}/>}/><div className="grid gap-5 lg:grid-cols-[1.1fr_.9fr]"><div className="space-y-5"><section className="surface-card p-5"><div className="grid grid-cols-2 gap-4 sm:grid-cols-4">{[[Clock,"Started",o.startTime],[RadioTower,"Reports",String(o.reports)],[ShieldCheck,"Consensus",`${o.consensus}%`],[BrainCircuit,"AI confidence",`${o.confidence}%`]].map(([Icon,l,v])=><div key={String(l)}><Icon className="h-4 w-4 text-primary"/><p className="mt-2 text-xs text-muted-foreground">{String(l)}</p><p className="mt-1 font-bold">{String(v)}</p></div>)}</div><div className="mt-6"><div className="flex justify-between text-xs"><span>Consensus threshold</span><strong>{o.consensus}%</strong></div><Progress value={o.consensus} className="mt-2"/></div></section><section className="surface-card p-5"><p className="text-sm font-semibold text-info">AI restoration estimate</p><p className="mt-2 text-lg font-semibold">{o.prediction}</p><p className="mt-2 text-xs text-muted-foreground">This prediction is an estimate based on historical and verified meter data. It is not a guarantee from an electricity distributor.</p></section><section className="surface-card p-5"><h2 className="font-semibold">Blockchain receipt</h2><p className="mt-1 text-xs text-muted-foreground">Simulated HSK Chain verification</p><div className="mt-4"><CopyHash hash={o.txHash} label={`Block #${o.block}`}/></div></section></div><section className="surface-card p-5"><h2 className="font-semibold">Event timeline</h2><p className="mt-1 text-xs text-muted-foreground">From first report to restoration intelligence</p><ActivityTimeline steps={steps} className="mt-7"/></section></div></>}
